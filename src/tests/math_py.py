#!python3
# supposed to be launced in docker image. MacOS doesn't have numpy
import numpy as np

def get_determ():

    # get random matrix
    # output random matrix in STDOUT
    # output det after matr in STDOUT

    return det

if __name__ == __main__: